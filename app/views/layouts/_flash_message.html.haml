#flash_message
  - flash.to_a.each do |name, msg|
    %div{ class: "center alert fade in alert-#{ name }" }
      %button.close{ "data-dismiss" => "alert" }×
      = content_tag :div, msg, id: "flash_#{name}"

