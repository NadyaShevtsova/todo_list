#loginModal.modal.show{"aria-hidden" => "true", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %h1.text-center Login
      .modal-body
        = render "layouts/flash_message"
        = form_for(resource, as: resource_name, url: session_path(resource_name), class: "form col-md-12 center-block") do |f|
          .form-group
            = f.email_field :email, placeholder: "User Email", class: "form-control input-lg", autofocus: true
          .form-group
            = f.password_field :password, placeholder: "Password", class: "form-control input-lg", autocomplete: "off"
          .form-group
            = f.submit "Sign in", class: "btn btn-primary btn-lg btn-block"
            %span.pull-right
              -# add here login via fb
            %span
              = render "devise/shared/links"
